"""Configuration constants for the briefing system."""

# Default text labels for different languages
DEFAULT_LABELS = {
    "zh": {
        "source_link": "消息来源"
    },
    "en": {
        "source_link": "Source"
    }
}

# Default configuration values
DEFAULT_CONFIG = {
    "language": "zh",
    "labels": DEFAULT_LABELS["zh"]
}